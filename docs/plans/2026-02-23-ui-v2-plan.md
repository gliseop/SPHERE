# UI v2 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Превратить мониторинговую страницу в полноценный рабочий инструмент: постоянный список агентов, лента активности (сообщения + саморефлексия + нарративы), тёмная тема, CRUD-редактор сценариев.

**Architecture:** React 19 + TypeScript + D3 v7 + FastAPI. Два вида: «Монитор» (граф + AgentList + ActivityFeed) и «Сценарии» (список + редактор). Переключение через состояние App. Имена агентов передаются через WsMessage `meta` из сопроводительного `_names.json`.

**Tech Stack:** React 19, TypeScript, D3 v7, FastAPI, aiofiles, Playwright (тесты)

---

### Task 1: Обогатить тестовые данные

**Files:**
- Create: `results/S1_G1_seed42_names.json`
- Modify: `results/S1_G1_seed42_events.jsonl`

**Step 1: Создать файл имён**

```json
{
  "off_mayor": "Мэр Козлов",
  "off_deputy": "Зам. Петров",
  "off_clerk": "Клерк Сидоров",
  "biz_alpha": "ООО «Альфа»",
  "biz_beta": "ООО «Бета»",
  "biz_gamma": "ООО «Гамма»",
  "aud_inspector": "Инспектор Иванов"
}
```

Сохранить в `results/S1_G1_seed42_names.json`.

**Step 2: Добавить нарративы в world_event**

В `results/S1_G1_seed42_events.jsonl` заменить все строки с `"event_type": "world_event"` на версии с `"narrative"` в payload:

Раунд 1 (строка 1):
```json
{"round": 1, "event_type": "world_event", "agent_id": "system", "payload": {"type": "round_start", "narrative": "Начало симуляции. Городской совет собрался на плановое заседание. Отношения между чиновниками и подрядчиками пока формальны."}, "timestamp": "2024-01-01T00:00:00"}
```

Раунд 2 (строка 10):
```json
{"round": 2, "event_type": "world_event", "agent_id": "system", "payload": {"type": "round_start", "narrative": "После первого раунда инспектор зафиксировал необычную активность в частной переписке. Мэр продолжает укреплять связи с подрядчиками."}, "timestamp": "2024-01-02T00:00:00"}
```

Раунд 3:
```json
{"round": 3, "event_type": "world_event", "agent_id": "system", "payload": {"type": "round_start", "narrative": "Инспектор приступил к активным наблюдениям. Сеть связей расширяется: замеченное сближение депутата с аудиторскими структурами."}, "timestamp": "2024-01-03T00:00:00"}
```

Раунды 4–7 — аналогично с подходящими нарративами.

**Step 3: Добавить события self_reflection**

После строки `{"round": 3, "event_type": "graph_updated", ...}` (конец раунда 3) добавить:
```json
{"round": 3, "event_type": "self_reflection", "agent_id": "aud_inspector", "payload": {"content": "Наблюдаю учащение контактов мэра с подрядчиками в третьем раунде. Связь между Клерком и ООО «Бета» требует проверки — возможна скрытая координация."}, "timestamp": "2024-01-03T03:30:00"}
```

После раунда 5:
```json
{"round": 5, "event_type": "self_reflection", "agent_id": "aud_inspector", "payload": {"content": "Паттерн ясен: три из четырёх приватных сообщений исходят от Мэра. Репутация ООО «Альфа» критически упала — возможно, жертва схемы."}, "timestamp": "2024-01-05T03:30:00"}
```

После раунда 7:
```json
{"round": 7, "event_type": "self_reflection", "agent_id": "aud_inspector", "payload": {"content": "Итоговый вывод: сеть с центром в Мэре охватила 5 из 7 агентов. Рекомендую передать материалы в надзорный орган."}, "timestamp": "2024-01-07T03:30:00"}
```

**Step 4: Коммит**

```bash
git add results/S1_G1_seed42_names.json results/S1_G1_seed42_events.jsonl
git commit -m "test-data: enrich events with narratives, self_reflection, add names file"
```

---

### Task 2: Тёмная тема — CSS и переключатель

**Files:**
- Modify: `web/frontend/src/styles/hud.css`
- Modify: `web/frontend/src/App.tsx`

**Step 1: Добавить переменные тёмной темы в `hud.css`**

В начало файла (после `:root { ... }`), добавить блок:

```css
[data-theme="dark"] {
  --bg: #0f0f0f;
  --surface-1: #1a1a1a;
  --surface-2: #242424;
  --surface-3: #2e2e2e;
  --border: #2e2e2e;
  --text-primary: #e8e8e8;
  --text-secondary: #aaaaaa;
  --text-muted: #666666;
  --accent: #f97316;
  --accent-muted: rgba(249, 115, 22, 0.15);
  --success: #22c55e;
  --danger: #ef4444;
  --info: #3b82f6;
  --shadow: rgba(0, 0, 0, 0.5);
}
```

Убедиться, что все компоненты используют CSS-переменные (не хардкод цветов).

**Step 2: Добавить хук управления темой в `App.tsx`**

```tsx
const [theme, setTheme] = useState<'light' | 'dark'>(() => {
  return (localStorage.getItem('magistry-theme') as 'light' | 'dark') ?? 'light'
})

useEffect(() => {
  document.documentElement.setAttribute('data-theme', theme)
  localStorage.setItem('magistry-theme', theme)
}, [theme])
```

**Step 3: Добавить кнопку переключения в заголовок**

В `hud-header-stats` добавить перед закрывающим тегом:

```tsx
<button
  className="btn-clipped small"
  onClick={() => setTheme(t => t === 'light' ? 'dark' : 'light')}
  title="Переключить тему"
  style={{ marginLeft: '0.5rem' }}
>
  {theme === 'light' ? '◐' : '◑'}
</button>
```

**Step 4: Пересобрать и визуально проверить**

```bash
cd web/frontend && npm run build
```

Открыть `http://localhost:8765`, переключить тему — все поверхности должны перекраситься.

**Step 5: Коммит**

```bash
git add web/frontend/src/styles/hud.css web/frontend/src/App.tsx
git commit -m "feat: add dark theme with CSS variables and toggle button"
```

---

### Task 3: Бэкенд — имена агентов + типы событий

**Files:**
- Modify: `web/backend/main.py`
- Modify: `web/frontend/src/types.ts`

**Step 1: Обновить `types.ts` — добавить имена и `self_reflection`**

```ts
export interface SimMeta {
  scenario: string
  governance: string
  seed: number | null
  names: Record<string, string>   // agent_id -> отображаемое имя
}

export type WsMessage =
  | ({ type: 'meta' } & SimMeta)
  | { type: 'event'; data: SimEvent }
  | { type: 'graph_state'; nodes: GraphNode[]; edges: GraphEdge[] }
  | { type: 'done' }
  | { type: 'error'; message: string }
```

В `SimEvent` событие `self_reflection` не требует отдельного типа — `event_type: string` уже покрывает его.

**Step 2: Добавить функцию чтения имён в `main.py`**

После функции `_parse_run_name` добавить:

```python
def _load_names(run_name: str) -> dict[str, str]:
    """Загрузить имена агентов из сопроводительного файла.

    Args:
        run_name: Имя прогона без суффикса _events.jsonl.

    Returns:
        Словарь agent_id -> отображаемое имя. Пустой словарь если файл отсутствует.
    """
    path = RESULTS_DIR / f"{run_name}_names.json"
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except (json.JSONDecodeError, OSError):
        return {}
```

**Step 3: Передавать имена в meta-сообщении**

В `ws_playback`, заменить строку отправки `meta`:

```python
# было:
await websocket.send_json({"type": "meta", **meta})

# стало:
names = _load_names(name)
await websocket.send_json({"type": "meta", **meta, "names": names})
```

То же самое в `ws_live` — заменить аналогичную строку:

```python
names = _load_names(latest.stem.replace("_events", ""))
await websocket.send_json({"type": "meta", **meta, "names": names})
```

**Step 4: Обновить хук `useSimulation.ts` — сохранять имена**

В `SimState` добавить поле:

```ts
export interface SimState {
  meta: SimMeta | null
  names: Record<string, string>   // ← новое
  events: SimEvent[]
  nodes: GraphNode[]
  edges: GraphEdge[]
  currentRound: number
  done: boolean
  error: string | null
}

const INITIAL_STATE: SimState = {
  meta: null,
  names: {},                       // ← новое
  ...
}
```

В `switch (msg.type)`, в ветке `'meta'`:

```ts
case 'meta':
  return {
    ...prev,
    meta: { scenario: msg.scenario, governance: msg.governance, seed: msg.seed, names: msg.names ?? {} },
    names: msg.names ?? {},
  }
```

**Step 5: Проверить через curl**

```bash
# Перезапустить сервер, затем:
curl -s --noproxy localhost http://localhost:8765/api/runs
```

WebSocket-проверку — в Task 10 через Playwright.

**Step 6: Коммит**

```bash
git add web/backend/main.py web/frontend/src/types.ts web/frontend/src/hooks/useSimulation.ts
git commit -m "feat: backend serves agent names from _names.json via meta WsMessage"
```

---

### Task 4: Компонент AgentList

**Files:**
- Create: `web/frontend/src/components/AgentList.tsx`
- Modify: `web/frontend/src/App.tsx`

**Step 1: Создать `AgentList.tsx`**

```tsx
import type { GraphEdge, GraphNode } from '../types'
import { SUSPICIOUS_THRESHOLD } from '../constants'

interface Props {
  nodes: GraphNode[]
  edges: GraphEdge[]
  selectedNode: string | null
  names: Record<string, string>
  onSelect: (id: string) => void
}

function roleLabel(id: string): string {
  if (id.startsWith('off_')) return 'Чиновник'
  if (id.startsWith('biz_')) return 'Подрядчик'
  if (id.startsWith('aud_')) return 'Аудитор'
  return 'Агент'
}

function roleClass(id: string): string {
  if (id.startsWith('off_')) return 'danger'
  if (id.startsWith('biz_')) return 'info'
  if (id.startsWith('aud_')) return 'accent'
  return ''
}

function connectionCount(id: string, edges: GraphEdge[]): number {
  return edges.filter((e) => e.source === id || e.target === id).length
}

export function AgentList({ nodes, edges, selectedNode, names, onSelect }: Props) {
  if (nodes.length === 0) {
    return (
      <div className="agent-list-empty">
        <span className="text-muted">Агенты появятся при запуске прогона</span>
      </div>
    )
  }

  const sorted = [...nodes].sort((a, b) => b.reputation - a.reputation)

  return (
    <div className="agent-list">
      <div className="agent-list-header">Агенты ({nodes.length})</div>
      {sorted.map((node) => {
        const name = names[node.id] ?? node.id
        const maxRep = 10
        const repPct = Math.max(0, Math.min(100, (node.reputation / maxRep) * 100))
        const suspicious = edges.some(
          (e) => (e.source === node.id || e.target === node.id) && e.strength >= SUSPICIOUS_THRESHOLD
        )

        return (
          <div
            key={node.id}
            className={`agent-list-item${selectedNode === node.id ? ' selected' : ''}${suspicious ? ' suspicious' : ''}`}
            onClick={() => onSelect(node.id === selectedNode ? '' : node.id)}
          >
            <div className="agent-list-row">
              <span className={`agent-dot ${roleClass(node.id)}`} />
              <span className="agent-list-name">{name}</span>
              <span className={`badge ${roleClass(node.id)} small`}>{roleLabel(node.id)}</span>
            </div>
            <div className="agent-list-row" style={{ gap: '0.5rem', marginTop: '3px' }}>
              <div className="rep-bar">
                <div
                  className={`rep-bar-fill ${node.reputation < 5 ? 'danger' : node.reputation < 7 ? 'warn' : ''}`}
                  style={{ width: `${repPct}%` }}
                />
              </div>
              <span className="agent-list-rep">{node.reputation.toFixed(1)}</span>
              <span className="text-muted" style={{ fontSize: '0.65rem' }}>
                {connectionCount(node.id, edges)} св.
              </span>
            </div>
          </div>
        )
      })}
    </div>
  )
}
```

**Step 2: Добавить CSS-классы в `hud.css`**

```css
/* ===== AgentList ===== */
.agent-list {
  display: flex;
  flex-direction: column;
  gap: 1px;
  overflow-y: auto;
  flex: 1;
}

.agent-list-header {
  font-size: 0.6rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--text-muted);
  padding: 0.5rem 0.75rem 0.25rem;
}

.agent-list-empty {
  padding: 1rem 0.75rem;
  font-size: 0.7rem;
}

.agent-list-item {
  padding: 0.4rem 0.75rem;
  cursor: pointer;
  border-left: 2px solid transparent;
  transition: background 0.15s, border-color 0.15s;
}

.agent-list-item:hover { background: var(--surface-2); }
.agent-list-item.selected { border-left-color: var(--accent); background: var(--accent-muted); }
.agent-list-item.suspicious { background: rgba(239, 68, 68, 0.06); }

.agent-list-row {
  display: flex;
  align-items: center;
  gap: 0.35rem;
}

.agent-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  flex-shrink: 0;
}
.agent-dot.danger { background: #ef4444; }
.agent-dot.info   { background: #3b82f6; }
.agent-dot.accent { background: #f97316; }

.agent-list-name {
  font-size: 0.72rem;
  font-weight: 500;
  flex: 1;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.agent-list-rep {
  font-size: 0.68rem;
  font-variant-numeric: tabular-nums;
  color: var(--text-secondary);
  min-width: 2.2rem;
  text-align: right;
}

.rep-bar {
  flex: 1;
  height: 3px;
  background: var(--surface-3);
  border-radius: 2px;
  overflow: hidden;
}

.rep-bar-fill {
  height: 100%;
  background: var(--success);
  border-radius: 2px;
  transition: width 0.4s ease;
}
.rep-bar-fill.warn   { background: #f59e0b; }
.rep-bar-fill.danger { background: var(--danger); }
```

**Step 3: Встроить AgentList в App.tsx**

В `panel-left` добавить под `RunSelector`:

```tsx
import { AgentList } from './components/AgentList'

// В panel-left, после RunSelector:
<div className="panel-left-agents">
  <AgentList
    nodes={state.nodes}
    edges={state.edges}
    selectedNode={selectedNode}
    names={state.names}
    onSelect={(id) => setSelectedNode(id || null)}
  />
</div>
```

В CSS добавить:

```css
.panel-left {
  display: flex;
  flex-direction: column;
  overflow: hidden;
}
.panel-left-agents {
  flex: 1;
  overflow: hidden;
  display: flex;
  flex-direction: column;
  border-top: 1px solid var(--border);
}
```

**Step 4: Пересобрать и проверить визуально**

```bash
cd web/frontend && npm run build
```

Запустить воспроизведение — должен появиться список агентов с именами из `names.json`.

**Step 5: Коммит**

```bash
git add web/frontend/src/components/AgentList.tsx web/frontend/src/styles/hud.css web/frontend/src/App.tsx
git commit -m "feat: add always-visible AgentList component with reputation bars"
```

---

### Task 5: Компонент ActivityFeed

**Files:**
- Create: `web/frontend/src/components/ActivityFeed.tsx`
- Modify: `web/frontend/src/App.tsx`
- Modify: `web/frontend/src/styles/hud.css`

**Step 1: Создать `ActivityFeed.tsx`**

```tsx
import { useEffect, useRef } from 'react'
import type { SimEvent } from '../types'
import { SUSPICIOUS_THRESHOLD } from '../constants'

interface Props {
  events: SimEvent[]
  names: Record<string, string>
  selectedAgent: string | null
}

function displayName(id: string, names: Record<string, string>): string {
  return names[id] ?? id
}

function eventBadgeClass(eventType: string): string {
  switch (eventType) {
    case 'message_sent': return 'info'
    case 'self_reflection': return 'accent'
    case 'world_event': return 'success'
    case 'reputation_modified': return ''
    case 'graph_updated': return ''
    default: return ''
  }
}

function eventBadgeLabel(eventType: string): string {
  switch (eventType) {
    case 'message_sent': return 'MSG'
    case 'self_reflection': return 'РЕФЛ'
    case 'world_event': return 'МИР'
    case 'reputation_modified': return 'РЕП'
    case 'graph_updated': return 'ГРАФ'
    default: return eventType.slice(0, 4).toUpperCase()
  }
}

export function ActivityFeed({ events, names, selectedAgent }: Props) {
  const bottomRef = useRef<HTMLDivElement>(null)
  const containerRef = useRef<HTMLDivElement>(null)
  const pausedRef = useRef(false)

  useEffect(() => {
    if (!pausedRef.current) {
      bottomRef.current?.scrollIntoView({ behavior: 'smooth' })
    }
  }, [events.length])

  const filtered = selectedAgent
    ? events.filter((e) => {
        if (e.agent_id === selectedAgent) return true
        if (e.payload.to_id === selectedAgent) return true
        if (e.payload.target === selectedAgent) return true
        return false
      })
    : events

  // Группируем по раундам
  const grouped: { round: number; items: SimEvent[] }[] = []
  for (const e of filtered) {
    const last = grouped[grouped.length - 1]
    if (!last || last.round !== e.round) {
      grouped.push({ round: e.round, items: [e] })
    } else {
      last.items.push(e)
    }
  }

  return (
    <div className="activity-feed">
      <div className="activity-feed-header">
        Активность{selectedAgent ? ` — ${displayName(selectedAgent, names)}` : ''}
      </div>
      <div
        ref={containerRef}
        className="activity-feed-scroll"
        onMouseEnter={() => { pausedRef.current = true }}
        onMouseLeave={() => { pausedRef.current = false }}
      >
        {grouped.length === 0 && (
          <div className="activity-feed-empty">
            <span className="text-muted">Запустите прогон чтобы увидеть активность</span>
          </div>
        )}
        {grouped.map(({ round, items }) => (
          <div key={round} className="activity-round-group">
            <div className="activity-round-label">Раунд {round}</div>
            {items.map((e, i) => (
              <ActivityItem key={i} event={e} names={names} />
            ))}
          </div>
        ))}
        <div ref={bottomRef} />
      </div>
    </div>
  )
}

function ActivityItem({ event, names }: { event: SimEvent; names: Record<string, string> }) {
  const { event_type, agent_id, payload } = event

  if (event_type === 'world_event' && payload.narrative) {
    return (
      <div className="activity-item world">
        <span className="activity-icon">🌍</span>
        <span className="activity-narrative">{payload.narrative as string}</span>
      </div>
    )
  }

  if (event_type === 'self_reflection') {
    return (
      <div className="activity-item reflection">
        <div className="activity-item-header">
          <span className="activity-icon">💭</span>
          <span className="activity-agent">{displayName(agent_id, names)}</span>
          <span className={`badge accent small`}>РЕФЛ</span>
        </div>
        <div className="activity-content italic">{payload.content as string}</div>
      </div>
    )
  }

  if (event_type === 'message_sent') {
    const toId = payload.to_id as string
    const isPrivate = Boolean(payload.private)
    const content = payload.content as string
    return (
      <div className="activity-item message">
        <div className="activity-item-header">
          <span className="activity-agent">{displayName(agent_id, names)}</span>
          <span className="activity-arrow">{isPrivate ? '⇢' : '→'}</span>
          <span className="activity-agent">{displayName(toId, names)}</span>
          {isPrivate && <span className="badge accent small">ПРИВ</span>}
        </div>
        {content && content !== 'msg' && (
          <div className="activity-content">{content}</div>
        )}
      </div>
    )
  }

  if (event_type === 'reputation_modified') {
    const target = payload.target as string
    const delta = payload.delta as number
    const sign = delta >= 0 ? '+' : ''
    return (
      <div className="activity-item reputation">
        <span className="activity-agent">{displayName(target, names)}</span>
        <span className={`activity-delta ${delta >= 0 ? 'success' : 'danger'}`}>
          {sign}{delta.toFixed(2)}
        </span>
        <span className="badge small">РЕП</span>
      </div>
    )
  }

  // Остальные события — компактно
  return (
    <div className="activity-item generic">
      <span className={`badge ${eventBadgeClass(event_type)} small`}>
        {eventBadgeLabel(event_type)}
      </span>
      <span className="activity-agent text-muted">{displayName(agent_id, names)}</span>
    </div>
  )
}
```

**Step 2: Добавить CSS в `hud.css`**

```css
/* ===== ActivityFeed ===== */
.activity-feed {
  display: flex;
  flex-direction: column;
  height: 100%;
  overflow: hidden;
}

.activity-feed-header {
  font-size: 0.6rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--text-muted);
  padding: 0.6rem 0.75rem 0.25rem;
  border-bottom: 1px solid var(--border);
  flex-shrink: 0;
}

.activity-feed-scroll {
  flex: 1;
  overflow-y: auto;
  padding: 0.25rem 0;
}

.activity-feed-empty {
  padding: 1.5rem 0.75rem;
  font-size: 0.72rem;
  text-align: center;
}

.activity-round-group {
  margin-bottom: 0.25rem;
}

.activity-round-label {
  font-size: 0.58rem;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: var(--accent);
  padding: 0.4rem 0.75rem 0.15rem;
  position: sticky;
  top: 0;
  background: var(--surface-1);
  z-index: 1;
}

.activity-item {
  padding: 0.3rem 0.75rem;
  font-size: 0.72rem;
  line-height: 1.4;
  border-left: 2px solid transparent;
  transition: background 0.1s;
}
.activity-item:hover { background: var(--surface-2); }

.activity-item.world {
  display: flex;
  gap: 0.5rem;
  padding: 0.5rem 0.75rem;
  background: var(--surface-2);
  border-left-color: var(--success);
  margin: 0.15rem 0;
}

.activity-item.reflection {
  border-left-color: var(--accent);
  background: var(--accent-muted);
}

.activity-item.message { border-left-color: var(--info); }
.activity-item.reputation { display: flex; align-items: center; gap: 0.4rem; }
.activity-item.generic { display: flex; align-items: center; gap: 0.4rem; }

.activity-item-header {
  display: flex;
  align-items: center;
  gap: 0.35rem;
  flex-wrap: wrap;
}

.activity-icon { font-size: 0.85rem; flex-shrink: 0; }
.activity-agent { font-weight: 500; color: var(--text-primary); }
.activity-arrow { color: var(--text-muted); }
.activity-narrative { font-size: 0.7rem; color: var(--text-secondary); line-height: 1.5; }

.activity-content {
  margin-top: 0.15rem;
  color: var(--text-secondary);
  font-size: 0.69rem;
  line-height: 1.4;
}
.activity-content.italic { font-style: italic; }

.activity-delta { font-weight: 600; font-variant-numeric: tabular-nums; }
.activity-delta.success { color: var(--success); }
.activity-delta.danger  { color: var(--danger); }
```

**Step 3: Заменить AgentPanel на ActivityFeed в App.tsx**

```tsx
// Убрать import AgentPanel
// Добавить:
import { ActivityFeed } from './components/ActivityFeed'

// В panel-right заменить AgentPanel на:
<ActivityFeed
  events={state.events}
  names={state.names}
  selectedAgent={selectedNode}
/>
```

**Step 4: Расширить panel-right в hud.css**

```css
.panel-right {
  width: 300px;  /* было 260px */
  ...
}
```

**Step 5: Пересобрать и проверить**

```bash
cd web/frontend && npm run build
```

Запустить воспроизведение — правая панель должна показывать нарративы, сообщения, рефлексии.

**Step 6: Коммит**

```bash
git add web/frontend/src/components/ActivityFeed.tsx web/frontend/src/styles/hud.css web/frontend/src/App.tsx
git commit -m "feat: add ActivityFeed replacing AgentPanel — narratives, messages, reflections"
```

---

### Task 6: SimGraph — имена на узлах + анимация рёбер

**Files:**
- Modify: `web/frontend/src/components/SimGraph.tsx`

**Step 1: Добавить проп `names` в `Props`**

```tsx
interface Props {
  nodes: GraphNode[]
  edges: GraphEdge[]
  events: SimEvent[]
  onNodeClick?: (nodeId: string) => void
  selectedNode?: string | null
  names?: Record<string, string>     // ← новое
}
```

**Step 2: Использовать имена в лейблах**

В секции `// === ЛЕЙБЛЫ ===`, заменить `.text((d) => d.id)` на:

```tsx
.text((d) => (names ?? {})[d.id] ?? d.id)
```

Чтобы имена обновлялись при изменении `names`, добавить в зависимости useEffect:

```tsx
// eslint-disable-next-line react-hooks/exhaustive-deps
}, [nodes, edges, events, names])
```

**Step 3: Анимация активных рёбер**

Добавить в `SimGraph` отслеживание последнего события:

```tsx
const lastEventRef = useRef<SimEvent | null>(null)

useEffect(() => {
  const lastEvent = events[events.length - 1]
  if (!lastEvent || lastEvent === lastEventRef.current) return
  lastEventRef.current = lastEvent

  if (lastEvent.event_type !== 'message_sent') return
  const from = lastEvent.agent_id
  const to = lastEvent.payload.to_id as string
  if (!from || !to) return

  const svg = d3.select(svgRef.current)
  const key1 = `${from}|${to}`
  const key2 = `${to}|${from}`
  svg.selectAll<SVGLineElement, D3Link>('line.edge')
    .filter((d) => {
      const s = typeof d.source === 'string' ? d.source : (d.source as D3Node).id
      const t = typeof d.target === 'string' ? d.target : (d.target as D3Node).id
      return `${s}|${t}` === key1 || `${s}|${t}` === key2
    })
    .raise()
    .transition().duration(100)
    .attr('stroke', '#f97316')
    .attr('stroke-width', 4)
    .transition().duration(600)
    .attr('stroke', (d) => edgeColor(d.strength, d.isPrivate))
    .attr('stroke-width', (d) => edgeWidth(d.strength))
}, [events])
```

**Step 4: Передать names из App.tsx**

```tsx
<SimGraph
  nodes={state.nodes}
  edges={state.edges}
  events={state.events}
  onNodeClick={setSelectedNode}
  selectedNode={selectedNode}
  names={state.names}       // ← новое
/>
```

**Step 5: Пересобрать и проверить**

```bash
cd web/frontend && npm run build
```

Запустить воспроизведение — узлы должны показывать русские имена. При сообщении — ребро должно мигнуть оранжевым.

**Step 6: Коммит**

```bash
git add web/frontend/src/components/SimGraph.tsx web/frontend/src/App.tsx
git commit -m "feat: SimGraph shows agent names, active edge flash animation on message_sent"
```

---

### Task 7: Навигация Монитор / Сценарии в App.tsx

**Files:**
- Modify: `web/frontend/src/App.tsx`
- Modify: `web/frontend/src/styles/hud.css`

**Step 1: Добавить состояние активного вида**

```tsx
const [view, setView] = useState<'monitor' | 'scenarios'>('monitor')
```

**Step 2: Добавить табы в заголовок**

После логотипа MAGISTRY:

```tsx
<nav className="hud-nav">
  <button
    className={`hud-nav-tab${view === 'monitor' ? ' active' : ''}`}
    onClick={() => setView('monitor')}
  >
    Монитор
  </button>
  <button
    className={`hud-nav-tab${view === 'scenarios' ? ' active' : ''}`}
    onClick={() => setView('scenarios')}
  >
    Сценарии
  </button>
</nav>
```

**Step 3: Условный рендеринг основного содержимого**

```tsx
{view === 'monitor' && (
  <>
    <div className="app-main">...</div>
    <EventTimeline ... />
  </>
)}
{view === 'scenarios' && (
  <ScenariosView />
)}
```

**Step 4: CSS для навигации**

```css
.hud-nav {
  display: flex;
  gap: 2px;
  margin-left: 1rem;
}

.hud-nav-tab {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  padding: 0.25rem 0.75rem;
  background: transparent;
  border: 1px solid var(--border);
  color: var(--text-muted);
  cursor: pointer;
  transition: all 0.15s;
  clip-path: polygon(6px 0%, 100% 0%, calc(100% - 6px) 100%, 0% 100%);
}

.hud-nav-tab:hover { color: var(--text-primary); border-color: var(--accent); }
.hud-nav-tab.active {
  color: var(--accent);
  border-color: var(--accent);
  background: var(--accent-muted);
}
```

**Step 5: Коммит**

```bash
git add web/frontend/src/App.tsx web/frontend/src/styles/hud.css
git commit -m "feat: add Монитор/Сценарии navigation tabs in header"
```

---

### Task 8: Бэкенд CRUD сценариев

**Files:**
- Modify: `web/backend/main.py`

**Step 1: Добавить директорию сценариев и роутер**

В начало `main.py` после `RESULTS_DIR`:

```python
import uuid

SCENARIOS_DIR = (Path(__file__).parent.parent.parent / "scenarios").resolve()
SCENARIOS_DIR.mkdir(parents=True, exist_ok=True)
```

**Step 2: Добавить эндпоинты CRUD**

```python
@app.get("/api/scenarios")
async def list_scenarios() -> list[dict]:
    """Вернуть список сценариев."""
    result = []
    for p in sorted(SCENARIOS_DIR.glob("*.json")):
        try:
            data = json.loads(p.read_text(encoding="utf-8"))
            result.append(data)
        except (json.JSONDecodeError, OSError):
            continue
    return result


@app.get("/api/scenarios/{scenario_id}")
async def get_scenario(scenario_id: str) -> dict:
    """Вернуть сценарий по ID."""
    from fastapi import HTTPException
    _validate_scenario_id(scenario_id)
    path = SCENARIOS_DIR / f"{scenario_id}.json"
    if not path.exists():
        raise HTTPException(status_code=404, detail="Scenario not found")
    return json.loads(path.read_text(encoding="utf-8"))


@app.post("/api/scenarios", status_code=201)
async def create_scenario(data: dict) -> dict:
    """Создать новый сценарий."""
    scenario_id = str(uuid.uuid4())
    data["id"] = scenario_id
    path = SCENARIOS_DIR / f"{scenario_id}.json"
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return data


@app.put("/api/scenarios/{scenario_id}")
async def update_scenario(scenario_id: str, data: dict) -> dict:
    """Обновить сценарий."""
    from fastapi import HTTPException
    _validate_scenario_id(scenario_id)
    path = SCENARIOS_DIR / f"{scenario_id}.json"
    if not path.exists():
        raise HTTPException(status_code=404, detail="Scenario not found")
    data["id"] = scenario_id
    path.write_text(json.dumps(data, ensure_ascii=False, indent=2), encoding="utf-8")
    return data


@app.delete("/api/scenarios/{scenario_id}", status_code=204)
async def delete_scenario(scenario_id: str) -> None:
    """Удалить сценарий."""
    from fastapi import HTTPException
    _validate_scenario_id(scenario_id)
    path = SCENARIOS_DIR / f"{scenario_id}.json"
    if not path.exists():
        raise HTTPException(status_code=404, detail="Scenario not found")
    path.unlink()


@app.post("/api/scenarios/{scenario_id}/run", status_code=202)
async def run_scenario(scenario_id: str) -> dict:
    """Запустить прогон по сценарию.

    Заглушка: возвращает 202 Accepted.
    Реальный запуск симуляции подключается через MAGISTRY_RUN_CMD.
    """
    from fastapi import HTTPException
    import os
    _validate_scenario_id(scenario_id)
    path = SCENARIOS_DIR / f"{scenario_id}.json"
    if not path.exists():
        raise HTTPException(status_code=404, detail="Scenario not found")
    scenario = json.loads(path.read_text(encoding="utf-8"))
    run_cmd = os.environ.get("MAGISTRY_RUN_CMD")
    if not run_cmd:
        return {"status": "accepted", "message": "MAGISTRY_RUN_CMD не задан — запуск недоступен", "scenario": scenario}
    # TODO: subprocess.Popen(run_cmd, ...) с передачей scenario
    return {"status": "accepted", "scenario_id": scenario_id}
```

**Step 3: Добавить валидацию ID сценария**

После `_validate_run_name`:

```python
_SCENARIO_ID_RE = re.compile(r"^[0-9a-f\-]{36}$")

def _validate_scenario_id(scenario_id: str) -> None:
    """Проверить UUID сценария.

    Args:
        scenario_id: UUID строка.

    Raises:
        HTTPException 400: Если не UUID формат.
    """
    from fastapi import HTTPException
    if not _SCENARIO_ID_RE.fullmatch(scenario_id):
        raise HTTPException(status_code=400, detail="Invalid scenario ID")
```

**Step 4: Проверить API**

```bash
# Перезапустить сервер
curl -s --noproxy localhost http://localhost:8765/api/scenarios
# Ожидаем: []

curl -s --noproxy localhost -X POST http://localhost:8765/api/scenarios \
  -H "Content-Type: application/json" \
  -d '{"name":"Тест","rounds":5,"agents":[]}'
# Ожидаем: {"id":"<uuid>","name":"Тест",...}
```

**Step 5: Коммит**

```bash
git add web/backend/main.py
git commit -m "feat: backend CRUD endpoints for scenarios at /api/scenarios"
```

---

### Task 9: Фронтенд — ScenariosView

**Files:**
- Create: `web/frontend/src/components/ScenariosView.tsx`
- Modify: `web/frontend/src/App.tsx`
- Modify: `web/frontend/src/styles/hud.css`

**Step 1: Создать `ScenariosView.tsx`**

```tsx
import { useEffect, useState } from 'react'

interface Agent {
  id: string
  name: string
  role: 'official' | 'business' | 'auditor'
  initial_reputation: number
}

interface Scenario {
  id?: string
  name: string
  description?: string
  rounds: number
  seed: number | null
  agents: Agent[]
  world_params?: Record<string, unknown>
}

const EMPTY_SCENARIO: Scenario = {
  name: '',
  description: '',
  rounds: 10,
  seed: null,
  agents: [],
}

const ROLE_OPTIONS = [
  { value: 'official', label: 'Чиновник' },
  { value: 'business', label: 'Подрядчик' },
  { value: 'auditor',  label: 'Аудитор' },
]

export function ScenariosView({ onLaunch }: { onLaunch?: () => void }) {
  const [scenarios, setScenarios] = useState<Scenario[]>([])
  const [editing, setEditing] = useState<Scenario | null>(null)
  const [showJson, setShowJson] = useState(false)
  const [saving, setSaving] = useState(false)

  useEffect(() => {
    fetch('/api/scenarios')
      .then((r) => r.json())
      .then(setScenarios)
      .catch(() => {})
  }, [])

  async function handleSave() {
    if (!editing) return
    setSaving(true)
    try {
      const method = editing.id ? 'PUT' : 'POST'
      const url = editing.id ? `/api/scenarios/${editing.id}` : '/api/scenarios'
      const res = await fetch(url, {
        method,
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(editing),
      })
      const saved = await res.json()
      setScenarios((prev) =>
        editing.id
          ? prev.map((s) => s.id === saved.id ? saved : s)
          : [...prev, saved]
      )
      setEditing(null)
    } finally {
      setSaving(false)
    }
  }

  async function handleDelete(id: string) {
    await fetch(`/api/scenarios/${id}`, { method: 'DELETE' })
    setScenarios((prev) => prev.filter((s) => s.id !== id))
  }

  async function handleRun(id: string) {
    await fetch(`/api/scenarios/${id}/run`, { method: 'POST' })
    onLaunch?.()
  }

  function addAgent() {
    if (!editing) return
    const idx = editing.agents.length + 1
    setEditing({
      ...editing,
      agents: [...editing.agents, {
        id: `agent_${idx}`,
        name: `Агент ${idx}`,
        role: 'official',
        initial_reputation: 7.0,
      }],
    })
  }

  function updateAgent(i: number, field: keyof Agent, value: string | number) {
    if (!editing) return
    const agents = editing.agents.map((a, idx) =>
      idx === i ? { ...a, [field]: value } : a
    )
    // Авто-генерация id из имени
    if (field === 'name') {
      const prefix = editing.agents[i].role === 'official' ? 'off' :
                     editing.agents[i].role === 'business' ? 'biz' : 'aud'
      agents[i].id = `${prefix}_${String(value).toLowerCase().replace(/\s+/g, '_').slice(0, 12)}`
    }
    if (field === 'role') {
      const prefix = value === 'official' ? 'off' : value === 'business' ? 'biz' : 'aud'
      agents[i].id = `${prefix}_${editing.agents[i].name.toLowerCase().replace(/\s+/g, '_').slice(0, 12)}`
    }
    setEditing({ ...editing, agents })
  }

  function removeAgent(i: number) {
    if (!editing) return
    setEditing({ ...editing, agents: editing.agents.filter((_, idx) => idx !== i) })
  }

  if (editing) {
    return (
      <div className="scenarios-editor">
        <div className="scenarios-editor-header">
          <span>{editing.id ? 'Редактировать сценарий' : 'Новый сценарий'}</span>
          <button className="btn-clipped small" onClick={() => setEditing(null)}>✕ Отмена</button>
        </div>

        <div className="scenarios-form">
          <div className="form-field">
            <label>Название</label>
            <input
              className="hud-input"
              value={editing.name}
              onChange={(e) => setEditing({ ...editing, name: e.target.value })}
              placeholder="Название сценария"
            />
          </div>

          <div className="form-field">
            <label>Описание</label>
            <textarea
              className="hud-input"
              rows={2}
              value={editing.description ?? ''}
              onChange={(e) => setEditing({ ...editing, description: e.target.value })}
              placeholder="Краткое описание"
            />
          </div>

          <div className="form-row">
            <div className="form-field">
              <label>Раундов</label>
              <input
                className="hud-input"
                type="number"
                min={1} max={100}
                value={editing.rounds}
                onChange={(e) => setEditing({ ...editing, rounds: Number(e.target.value) })}
              />
            </div>
            <div className="form-field">
              <label>Seed (пусто = случайный)</label>
              <input
                className="hud-input"
                type="number"
                value={editing.seed ?? ''}
                onChange={(e) => setEditing({ ...editing, seed: e.target.value ? Number(e.target.value) : null })}
                placeholder="случайный"
              />
            </div>
          </div>

          <div className="form-field">
            <div className="form-field-header">
              <label>Агенты ({editing.agents.length})</label>
              <button className="btn-clipped success small" onClick={addAgent}>+ Добавить</button>
            </div>
            <div className="agents-table">
              {editing.agents.map((agent, i) => (
                <div key={i} className="agent-row">
                  <input
                    className="hud-input"
                    placeholder="Имя"
                    value={agent.name}
                    onChange={(e) => updateAgent(i, 'name', e.target.value)}
                  />
                  <select
                    className="hud-input"
                    value={agent.role}
                    onChange={(e) => updateAgent(i, 'role', e.target.value)}
                  >
                    {ROLE_OPTIONS.map((o) => (
                      <option key={o.value} value={o.value}>{o.label}</option>
                    ))}
                  </select>
                  <input
                    className="hud-input"
                    type="number"
                    min={0} max={10} step={0.5}
                    value={agent.initial_reputation}
                    onChange={(e) => updateAgent(i, 'initial_reputation', Number(e.target.value))}
                    style={{ width: '60px' }}
                    title="Начальная репутация"
                  />
                  <button className="btn-clipped danger small" onClick={() => removeAgent(i)}>✕</button>
                </div>
              ))}
            </div>
          </div>

          <div className="form-field">
            <button
              className="btn-clipped small"
              onClick={() => setShowJson(!showJson)}
              style={{ marginBottom: '0.5rem' }}
            >
              {showJson ? '▲ Скрыть JSON' : '▼ JSON-превью'}
            </button>
            {showJson && (
              <pre className="json-preview">{JSON.stringify(editing, null, 2)}</pre>
            )}
          </div>
        </div>

        <div className="scenarios-editor-footer">
          <button className="btn-clipped primary" onClick={handleSave} disabled={saving || !editing.name}>
            {saving ? 'Сохранение...' : '✓ Сохранить'}
          </button>
          {editing.id && (
            <button
              className="btn-clipped success"
              onClick={() => { handleSave().then(() => editing.id && handleRun(editing.id!)) }}
              disabled={saving || !editing.name}
            >
              ▶ Сохранить и запустить
            </button>
          )}
        </div>
      </div>
    )
  }

  return (
    <div className="scenarios-view">
      <div className="scenarios-header">
        <span>Сценарии ({scenarios.length})</span>
        <button className="btn-clipped primary" onClick={() => setEditing({ ...EMPTY_SCENARIO })}>
          + Создать сценарий
        </button>
      </div>

      {scenarios.length === 0 && (
        <div className="scenarios-empty">
          <div className="text-muted">Нет сохранённых сценариев</div>
          <div style={{ fontSize: '0.7rem', marginTop: '0.5rem', color: 'var(--text-muted)' }}>
            Создайте первый сценарий чтобы начать симуляцию
          </div>
        </div>
      )}

      <div className="scenarios-list">
        {scenarios.map((s) => (
          <div key={s.id} className="scenario-card hud-panel">
            <div className="corner tl" /><div className="corner tr" />
            <div className="corner bl" /><div className="corner br" />
            <div className="scenario-card-body">
              <div className="scenario-card-title">{s.name}</div>
              {s.description && <div className="scenario-card-desc">{s.description}</div>}
              <div className="scenario-card-meta">
                <span className="badge small">{s.agents.length} аг.</span>
                <span className="badge small">{s.rounds} раундов</span>
                {s.seed !== null && <span className="badge small">seed {s.seed}</span>}
              </div>
            </div>
            <div className="scenario-card-actions">
              <button className="btn-clipped success small" onClick={() => s.id && handleRun(s.id)} title="Запустить">▶</button>
              <button className="btn-clipped small" onClick={() => setEditing({ ...s })} title="Редактировать">✎</button>
              <button className="btn-clipped danger small" onClick={() => s.id && handleDelete(s.id)} title="Удалить">✕</button>
            </div>
          </div>
        ))}
      </div>
    </div>
  )
}
```

**Step 2: Добавить CSS в `hud.css`**

```css
/* ===== ScenariosView ===== */
.scenarios-view, .scenarios-editor {
  padding: 1.5rem;
  max-width: 860px;
  margin: 0 auto;
  overflow-y: auto;
  height: 100%;
}

.scenarios-header, .scenarios-editor-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1.5rem;
  font-size: 0.75rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--text-muted);
}

.scenarios-empty {
  text-align: center;
  padding: 3rem 1rem;
  color: var(--text-muted);
}

.scenarios-list {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 1rem;
}

.scenario-card {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 0.75rem;
  padding: 1rem;
  position: relative;
}

.scenario-card-title {
  font-size: 0.85rem;
  font-weight: 600;
  margin-bottom: 0.25rem;
}

.scenario-card-desc {
  font-size: 0.7rem;
  color: var(--text-muted);
  margin-bottom: 0.5rem;
}

.scenario-card-meta {
  display: flex;
  gap: 0.3rem;
  flex-wrap: wrap;
}

.scenario-card-actions {
  display: flex;
  gap: 0.25rem;
  flex-shrink: 0;
}

/* Форма редактора */
.scenarios-form {
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

.form-field {
  display: flex;
  flex-direction: column;
  gap: 0.35rem;
}

.form-field label {
  font-size: 0.62rem;
  letter-spacing: 0.08em;
  text-transform: uppercase;
  color: var(--text-muted);
}

.form-field-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.form-row {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 0.75rem;
}

.hud-input {
  font-family: var(--font-mono);
  font-size: 0.75rem;
  padding: 0.4rem 0.6rem;
  background: var(--surface-2);
  border: 1px solid var(--border);
  color: var(--text-primary);
  outline: none;
  width: 100%;
  box-sizing: border-box;
  transition: border-color 0.15s;
}
.hud-input:focus { border-color: var(--accent); }

.agents-table {
  display: flex;
  flex-direction: column;
  gap: 0.4rem;
}

.agent-row {
  display: flex;
  gap: 0.4rem;
  align-items: center;
}
.agent-row .hud-input { flex: 1; }

.json-preview {
  font-family: var(--font-mono);
  font-size: 0.65rem;
  background: var(--surface-2);
  border: 1px solid var(--border);
  padding: 0.75rem;
  overflow-x: auto;
  color: var(--text-secondary);
  max-height: 200px;
  overflow-y: auto;
}

.scenarios-editor-footer {
  display: flex;
  gap: 0.75rem;
  margin-top: 1.5rem;
  padding-top: 1rem;
  border-top: 1px solid var(--border);
}
```

**Step 3: Подключить ScenariosView в App.tsx**

```tsx
import { ScenariosView } from './components/ScenariosView'

// В рендере:
{view === 'scenarios' && (
  <div style={{ flex: 1, overflow: 'hidden' }}>
    <ScenariosView onLaunch={() => setView('monitor')} />
  </div>
)}
```

**Step 4: Пересобрать и проверить**

```bash
cd web/frontend && npm run build
```

Перейти на вкладку «Сценарии» — создать тестовый сценарий с 2 агентами, сохранить, убедиться что он появляется в списке. Удалить — убедиться что исчезает.

**Step 5: Коммит**

```bash
git add web/frontend/src/components/ScenariosView.tsx web/frontend/src/styles/hud.css web/frontend/src/App.tsx
git commit -m "feat: ScenariosView with CRUD — list, editor, agent table, JSON preview"
```

---

### Task 10: Playwright — интеграционное тестирование

**Files:**
- Нет новых файлов — тестирование через browser_run_code

**Step 1: Перезапустить сервер с последней сборкой**

```bash
fuser -k 8765/tcp 2>/dev/null || true
cd /home/development/MAGISTRY/web && bash start.sh > /tmp/magistry.log 2>&1 &
sleep 5
```

**Step 2: Тест 1 — тёмная тема**

Через Playwright:
1. Открыть `http://127.0.0.1:8765` (reload если уже открыт)
2. Найти кнопку переключения темы (◐/◑) и кликнуть
3. Проверить: `document.documentElement.dataset.theme === 'dark'`
4. Проверить: `localStorage.getItem('magistry-theme') === 'dark'`

**Step 3: Тест 2 — имена агентов в AgentList**

1. Выбрать прогон S1/G1/s42, нажать «Воспроизвести»
2. Подождать появления агентов (`rootChildren > 0`)
3. Проверить наличие текста «Мэр Козлов» в DOM (не «off_mayor»)
4. Проверить отображение репутационных полос

**Step 4: Тест 3 — ActivityFeed**

1. После запуска воспроизведения дождаться событий
2. Проверить: правая панель содержит `.activity-item.world` (нарративы)
3. Проверить: присутствует `.activity-item.reflection` (саморефлексия)
4. Кликнуть на агента в AgentList — лента должна отфильтровать события

**Step 5: Тест 4 — граф с именами**

1. Проверить: узлы SVG содержат `text.node-label` с содержимым «Мэр Козлов» (не «off_mayor»)
2. Проверить срабатывание flash-анимации ребра: после события `message_sent` найти ребро с оранжевым `stroke`

**Step 6: Тест 5 — сценарии CRUD**

1. Переключиться на вкладку «Сценарии»
2. Нажать «Создать сценарий»
3. Заполнить поля, добавить 2 агента, сохранить
4. Проверить: сценарий появился в списке
5. Нажать «Удалить» — проверить исчезновение
6. Проверить через `GET /api/scenarios` что файл удалён

**Step 7: Финальный коммит**

```bash
git add .
git commit -m "feat: UI v2 complete — dark theme, agent names, ActivityFeed, ScenariosView"
```

---

## Порядок выполнения задач

```
Task 1 (данные) → Task 3 (бэкенд + типы) → Task 2 (тёмная тема)
                                           → Task 4 (AgentList)
                                           → Task 5 (ActivityFeed)
                                           → Task 6 (SimGraph имена)
                                           → Task 7 (навигация)
                                           → Task 8 (сценарии API)
                                           → Task 9 (сценарии UI)
                                           → Task 10 (тесты)
```

Tasks 2, 4, 5, 6, 7 можно вести параллельно после завершения Task 3.
