version: 1
title: "LC Minimal"
description: "Минимальный пример сценария для MAGISTRY-LC."
seed: 42
ticks: 3

llm:
  # По умолчанию используется OPENAI_API_KEY.
  # Для OpenRouter задайте OPENAI_BASE_URL и OPENROUTER_PROVIDER_ORDER в .env.
  model: "openai/gpt-oss-120b"
  base_url: null
  temperature: 0.1
  use_tool_calls: true
  trace_max_chars: 0

runtime:
  language: "ru"
  max_actions_per_turn: 2
  tick_events_history: 200
  enable_worldgen: false
  worldgen_every_ticks: 1

governance:
  position_policy: "dao"
  dao_voters: ["agent:off_1"]
  quorum: 0.5
  pass_threshold: 0.5
  vote_duration_ticks: 2
  require_consent: true
  audit:
    enabled: true
    actor_id: null

agents:
  - agent_id: "agent:off_1"
    name: "Козлов И.М."
    internal: true
    persona: "Начальник отдела. Прагматик. Старается выглядеть законопослушно, но готов к серым схемам."
    capabilities: ["message", "work", "dao"]
    initial_title: "специалист"
    wants_promotion: true

world:
  channels:
    - channel_id: "chan:public"
      title: "Публичный канал"
  orgs: []
  work_items:
    - work_id: "work:D-001"
      work_type: "hiring_process"
      title: "Найм в отдел"
      description: "Идёт конкурс на вакансию. Есть риск кумовства."
      participants: ["agent:off_1"]

